This is a simple login form with a wave effect on the labels.
So, if you go ahead and click in the input, you'll see that 
the label will then wave up.

The main tool that causes this effect is the <span> that wraps
each label by using Javascript.